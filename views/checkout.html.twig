<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Checkout</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    </head>
    <body>
        <div class="container">
            {% if books is not empty %}
            <ul>
                {% for book in books %}
                <li><a href="/bookcheckout/{{ book.getId }}">{{ book.getTitle }}</a></li>
                {% endfor %}
            </ul>
            {% endif %}
        </div>
    </body>
</html>
